import React, {useState, useEffect} from 'react';
import {firebase} from './../firebase';
import {MainContext} from './../context';

export const OrderView = ({match}) => {
    return (
        <div className="order__view">
            this is the order vie page
        </div>
    )
}